name: Manual Workflow
on:
  workflow_dispatch:
jobs:
  run-manual-tasks:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
    - name: Info 
      run: |
        echo "running manual tasks
